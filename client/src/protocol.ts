// ホストとクライアントのメッセージ仕様。互換性を壊す変更時に上げる。
//
// ホストもICE候補をtrickleで送るようになった (offerに候補が入らなくなった) が、
// 版は据え置き。片側だけ古いと、バージョン不一致ではなく候補0件のofferとして
// ICEが黙って失敗するので、クライアント(Pages)とホストは必ず同時に入れ替える。
export const PROTOCOL_VERSION = 1;
